<% content_for :page_title, t('views.new.title', resource: User.model_name.human) %>
<% content_for :actions do %>
  <%= link_to(t('views.actions.back'), users_path,
    class: 'btn btn-sm btn-outline-secondary') %>
<% end %>

<%= render 'form', user: @user %>
